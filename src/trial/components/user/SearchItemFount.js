
import PropTypes from 'prop-types';

import { useState } from 'react';

import 

function ItemFound({searchValue,}){

     const [ state, setState ] = useState(
     	  {
     	  	found:'',
     	  }
     );

     const { found, } = state;

	 return (
       <div>
       	
       </div>  
	 )
}